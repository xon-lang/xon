check excess useGenerics methods

многострочные комментарии
"""
    |a
    |b
"""

добавить not для типов (f(f not String): --)
добавить binding на уровне языка чтобы отловить изменения свойств (React, Angular)

TimeSpan(nanoseconds)
  nanoseconds Float64
  milliseconds = ns * 10^3
  seconds = ns * 10^6
  minutes = ns * 10^6

Посмотреть как в 1С сделаны multiline c dsl для запросов