Integer is Float
  prefix + () Integer
  prefix - () Integer
  infix + (other Integer) Integer
  infix - (other Integer) Integer
  infix * (other Integer) Integer
  infix / (other Integer) Float
  infix ^ (other Integer) Integer
  infix > (other Integer) Boolean
  infix < (other Integer) Boolean
  infix == (other Integer) Boolean

  toString() String
