ParameterTree(context) is Node<ParameterContext>:
  name = context._name.text
  type = context.type().toNode()
  isPrivate = $name.startsWith('_')
  metaType = context._meta?.text

  toString() = `{$name} {$type}`
