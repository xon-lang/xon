ParenthesizedTypeNode is TypeNode
  baseType = ctx.type().toNode()
  generics = [$baseType]

  infix ==(other TypeNode) other is ParenthesizedTypeNode and baseType == other

  useGenericsMap(genericsMap Map<string, TypeNode>) =
    createParenthesizedType(baseType.useGenericsMap(genericsMap))

  getGenericsMap(type TypeNode) = baseType.getGenericsMap(type)

  toString() = `({baseType.toString()})`
