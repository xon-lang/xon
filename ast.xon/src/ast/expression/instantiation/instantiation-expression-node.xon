InstantiationExpressionNode(context) is ExpressionNode
  type = createPlainType(context.id().text, context.generics()?.type().toNodes())
  arguments = context.arguments().toNodes()

  toString() = `{$type}({$arguments.join(', ')})`
