callLambda(\x, y => x + y)
callLambda(x, y => x + y)

call($=>$0+$1)
call(\$0+$1)
call(~$0+$1)

call($0+$1)

call({$0+$1})


2+3 | @ + @.before()



2+3 | $ + $.before()

2+3, 4 + 6 |x x0 + x1

\x, y: y, y


a = func(\x,y: get(@, @ + 5) |x: get($ + 234, $) |z: get($, $ | x + $))
