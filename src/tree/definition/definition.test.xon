SomeClass is BaseClass<String, Boolean>:
    property String
    anotherProp Number
    typedValue Number
    
    @(name String):
        //

    method() MyType:
        hello()
        bye()
    
    location(x Number, y Number) Position:
        coord(x, y)

    operator + (sc SomeClass) SomeClass:
        log("fine!")