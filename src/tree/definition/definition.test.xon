SomeClass:

    ...BaseClass<String, Boolean>
    ...List<Number>

    property String
    anotherProp Number
    typed_value Number
    
    method():
        hello()
        bye()
    
    location(x Number, y Number) Position:
        coord(x, y)

    infix + (sc SomeClass):
        log('fine!')