SomeClass is BaseClass<String, Boolean>:
    property String
    anotherProp Number
    typedValue Number
    
    @(name String):
        //

    method() MyType:
        hello()
        bye()
    
    location(x Number, y Number) Position:
        pos(x, y)

    + (it SomeClass, sc SomeClass) SomeClass:
        log("fine!")