SomeClass is BaseClass<String, Boolean>:
    property String
    anotherProp Number
    typed_value Number
    
    method() MyType:
        hello()
        bye()
    
    location(x Number, y Number) Position:
        coord(x, y)

    infix + (sc SomeClass) SomeClass:
        log("fine!")