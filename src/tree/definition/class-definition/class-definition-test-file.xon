SomeClass<T>(name String) is BaseClass<String, Boolean>:
    property: 123
    anotherProp Number
    typedValue Number

    method() MyType:
        hello()
        bye()

    location(x Number, y Number) Position:
        pos(x, y)

    infix + (it SomeClass, sc SomeClass) SomeClass:
        log("fine!")