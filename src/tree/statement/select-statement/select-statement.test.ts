test('variable assignment', () => {
    // const code = 'a = 5 + 5\n';
    // const tree = parseCode(code, SelectStatementTree);
    // expect(tree.name).toBe('a');
    // expect(evalExpression(tree.value)).toBe(5 + 5);
});
