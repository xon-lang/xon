return 1213

object someObjectFactory<|T|>(name String)
  arr Number[] = [123, 456, 789, 456, 789]
  abc Integer

  location(x Number, y Number) Position
    pos(x, y)

  operator + (it SomeClass) AnotherClass
    log('fine!')

model Array<|T|>
  operator + (other T[]) T[]
  operator * (other Integer) T[][]
