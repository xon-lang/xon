SomeClass<T>(name String) is BaseClass<String, Boolean>(name, 1)
  anotherProp String
  typedValue Number

  property = 123

  method () MyType
    hello()
    bye()

  location (x Number, y Number) Position
    pos(x, y)

  infix + (it SomeClass) AnotherClass
    log("fine!")
