String:
    + (a, b Boolean) String
    + (a, b Integer) String
    + (a, b Float) String
    + (a, b Char) String
    + (a, b String) String

    // - (a, b Boolean) Number
    // - (a, b Number) Number
    // - (a, b String) String

    // * (a, b Boolean) Number
    // * (a, b Number) Number
    // * (a, b String) String

    // / (a, b Boolean) Number
    // / (a, b Number) Number
    // / (a, b String) String