Integer is Number:
    + (x Boolean) Integer
    + (x Integer) Integer
    + (x Float) Float
    + (x Char) Integer
    + (x String) String

    - (x Boolean) Integer
    - (x Integer) Integer
    - (x Float) Float
    - (x Char) Integer
    // - (x String) String

    * (x Boolean) Integer
    * (x Integer) Integer
    * (x Float) Float
    * (x Char) Integer
    // * (x String) String

    / (x Boolean) Integer
    / (x Integer) Integer
    / (x Float) Float
    / (x Char) Integer
    // / (x String) String

    // (Integer#Literal + Integer#Literal) Integer#Literal:
    //     return a.value + b.value
