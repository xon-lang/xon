SimpleClass:
    _prop Integer
    tryped Number
    only_typed String

    method(t Number, v Integer, vt String)
        render_plugin()

    location(a Complex, b Number)
        @_coord(x, y)

    _coord(x Number, y Number)
        -- console.log(1)