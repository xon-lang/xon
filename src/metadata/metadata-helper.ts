import { PlainTypeTree } from "../tree/type/plain-type/plain-type.tree";
import { TypeTree } from "../tree/type/type.tree";

export function nameToMetadata(name: string, dependency:string){

}

export function typeTreeToMetadata(type: TypeTree){
  if(type instanceof PlainTypeTree) 
}